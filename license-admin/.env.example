# Se deja vacío en producción si el panel es servido por el mismo license-server (mismo origen)
# En entorno de desarrollo (dev), apunta al servidor Express local:
VITE_LICENSE_API_URL=http://localhost:4000
